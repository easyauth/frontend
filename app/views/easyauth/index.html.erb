  <!-- Navigation -->
  <div id="nav-placeholder"></div>
  <!-- Page Content -->
    <div class="row">
      <div class="col-md-3">
        <p class="lead">Hello, <%= unless session[:name].nil? then session[:name] else "user" end %>!</p>
        <div class="list-group">
          <a href=
            <%="/" %>
          class="list-group-item active">Home</a> <% if session[:authenticated] %>
          <a href=
          <%="/create_cert" %>
          class="list-group-item">Create CSR</a> 
          <a href=
          <%="/profile/#{session[:id]}" %>
          class="list-group-item">Edit Profile</a>
          <% end %>
        </div>
      </div>
      <div class="col-md-9">


        <div class="thumbnail">
          <div class="caption-full">
            <h4 id="blueh4">Welcome to Easyauth</h4>
            <p>We aim to make client certificate authentication easy for everyone.</p>
          </div>
        </div>
      </div>
    </div>

